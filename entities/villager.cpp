#include "villager.h"

/*

		virtual ~Villager();
		bool onInit() override;
		void onUpdate(float delta) override;
		void onRender() override;

		void onWindowResize(int width, int height) override;
		void onKeyEvent(int key, int scanCode, int action, int mods) override;
		void onMouseButtonEvent(const MouseButtonEvent& event) override;
		void onMouseMoveEvent(const MouseMoveEvent& event) override;

*/

namespace aoe_engine {

	Villager::~Villager() {
	}

	bool Villager::onInit() {
		return true;
	}

	void Villager::onUpdate(float delta) {
	}

	void Villager::onRender() {
	}

	void Villager::onWindowResize(int width, int height) {
	}

	void Villager::onKeyEvent(int key, int scanCode, int action, int mods) {
	}

	void Villager::onMouseButtonEvent(const MouseButtonEvent& event) {
	}

	void Villager::onMouseMoveEvent(const MouseMoveEvent& event) {
	}

}